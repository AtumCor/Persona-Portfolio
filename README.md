# Persona-Portfolio

To run the project locally, use the following commands:

Pre-requisites
Have nvm and node installed

Install dependencies
```bash
npm install
```

To start the server locally
```bash
npm run dev
```

The design language of this project follows the principles of Persona the video game. 
It's heavily inspired by the visual style, color schemes, and UI elements found in the Persona series.
